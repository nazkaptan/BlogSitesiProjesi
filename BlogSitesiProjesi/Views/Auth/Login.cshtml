@model LoginViewModel
@{
    ViewData["Title"] = "Giris yap";
}

<link href="~/css/auth.css" rel="stylesheet" />

<div class="wrapper fadeInDown">
    <div id="formContent">
        <!-- Tabs Titles -->
        <!-- Icon -->
        <div class="fadeIn first">
            <img src="https://www.kindpng.com/picc/m/24-248394_user-icon-hd-png-download.png" id="icon" alt="User Icon" />
            <br />
            <h2 style="margin:0px;">Giris Yap</h2>
        </div>
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        @if (TempData["message"] != null)
        {
            <div class="text-success">@TempData["message"]</div>
        }

        <!-- Login Form -->
        <form asp-action="Login" asp-controller="Auth" method="post" asp-route-yonlen="@ViewBag.yonlen">
            <input asp-for="Email" class="fadeIn second" placeholder="E-mail gir" />
            <br />
            <span asp-validation-for="Email" class="text-danger"></span>

            <input type="submit" value="Giris yap" class="fadeIn fourth" />
        </form>

        @*Register?*@
        <div id="formFooter">
            <a asp-action="Register" asp-controller="Auth" class="underlineHover">Kayit olmak ister misin?</a>
        </div>
    </div>
</div>